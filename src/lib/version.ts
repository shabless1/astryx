/**
 * ASTRYX — single source of truth for the app version (Phase 7).
 * Update HERE only; About/footer/metadata all import from this.
 * Keep in sync with the user manual (v1.4).
 */
export const APP_VERSION = '1.4.0'
export const APP_VERSION_LABEL = `v${APP_VERSION}`
