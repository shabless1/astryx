/**
 * ASTRYX — Legal config barrel (LEGAL SHIELD v1).
 *
 * Import legal copy from `@/legal` anywhere in the app. All strings resolve to
 * src/legal/copy.ts — the single file counsel edits.
 */
export * from './copy'
